<UserControl x:Class="ThicknessAndComposition_Inspector_IPS.UC_AnalysisMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThicknessAndComposition_Inspector_IPS"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <DockPanel Name="dckPanel" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border CornerRadius="6" BorderThickness="2" BorderBrush="RoyalBlue" DockPanel.Dock="Top" Background="Black" >
            <Grid> 
                <Canvas Grid.Column="0" ClipToBounds="True" Margin="15" >
                    <Canvas.Background>
                        <ImageBrush x:Name="imgMap" Stretch="UniformToFill"  RenderOptions.BitmapScalingMode="Fant"   />
                    </Canvas.Background>
                </Canvas>

                <Canvas Name="cvsMap" Grid.Column="0" Margin="15" />



            </Grid>
        </Border>
    </DockPanel>
</UserControl>
